function k-map(list,num)
    n = len(list) #Determine the length of the K-Map to chart the entire K-map
    list_placeholder = list #Copy list for manipulation into appropriate position for K-Map calculation
    if n == 4:
        list[4] = list[3];
        list[3] = list[4];
    else:
        print("To Do")
    end
    